version: '3.8'

services:
  chaotic-inbox:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8030:80'
    restart: unless-stopped
    environment:
      - NODE_ENV=production
